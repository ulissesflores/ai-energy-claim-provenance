[project]
name = "ai-energy-claim-provenance"
version = "0.1.0"
requires-python = ">=3.11"
description = "Companion repository for 'O Brasil virou destino de data center e não mede o que eles gastam': every number the article calculates itself, recomputed from its primary source and locked by a test."
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [
  { name = "Carlos Ulisses Flores" }
]

[tool.ruff]
line-length = 100
target-version = "py311"
fix = false
show-fixes = true

[tool.ruff.lint]
select = [
  "E", "F", "W",        # pycodestyle/pyflakes
  "I",                  # isort
  "UP",                 # pyupgrade
  "B",                  # bugbear
  "D",                  # pydocstyle
]
ignore = [
  "D203",  # 1 blank line required before class docstring (conflicts with D211)
  "D213",  # Multi-line docstring summary should start at the second line (conflicts with D212)
]
pydocstyle.convention = "numpy"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

[tool.interrogate]
ignore-init-method = true
ignore-init-module = true
ignore-magic = true
ignore-module = false
fail-under = 100
verbose = 2

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
